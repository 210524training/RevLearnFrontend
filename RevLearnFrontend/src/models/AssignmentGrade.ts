/**
 * Represents a users grade in a assignment.
 * @property assignmentID: string
 * @property grade: number
 */
export interface AssignmentGrade {
  assignmentID: string,
  grade: number | string,
}
